# Phase P0 runtime deps — minimal local, free, offline-friendly.
fastapi
uvicorn[standard]
pydantic>=2.0
python-multipart
pymupdf
jsonschema

# --- Later phases (uncomment when implementing) ---
# paddleocr        # scanned-PDF OCR fallback (Phase P1)
# paddlepaddle
# spacy            # NLP structuring (Phase P1)
# spacy-transformers
# openai           # optional LLM judge (Phase P3)